# 加密日记本 📔

一个使用 Astro + Tailwind CSS 构建的私密加密日记网站。

## 特性

- 🔐 密码保护：使用 AES 加密保护您的日记内容
- 📱 响应式设计：完美适配各种设备
- 🎨 美观界面：使用 Tailwind CSS 打造现代化 UI
- 🔒 数据安全：原始日记数据存储在私有子模块中
- 🚀 自动部署：通过 GitHub Actions 自动构建和部署

## 本地开发

### 1. 安装依赖

```bash
npm install
```

### 2. 设置密码

创建 `.env` 文件：

```bash
DIARY_PASSWORD=your_secret_password
```

### 3. 加密日记数据

```bash
node scripts/encrypt-diaries.js
```

这会生成一个密码哈希，将其复制到 `src/components/Login.astro` 中的 `PASSWORD_HASH`。

### 4. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:4321

## 日记格式

在 `diary` 子模块中创建 markdown 文件，支持以下格式：

### 带 Frontmatter

```markdown
---
title: 我的日记标题
date: 2025-11-06
tags: [生活, 随笔]
---

这是日记内容...
```

### 简单格式

```markdown
# 我的日记标题

这是日记内容...
```

文件名可以包含日期：`2025-11-06-title.md`

## 部署到 GitHub Pages

### 1. 设置 GitHub Secret

在仓库设置中添加 Secret：
- Name: `DIARY_PASSWORD`
- Value: 你的密码

### 2. 启用 GitHub Pages

1. 进入仓库 Settings → Pages
2. Source 选择 "GitHub Actions"

### 3. 推送代码

```bash
git add .
git commit -m "Setup Astro diary site"
git push
```

GitHub Actions 会自动构建和部署。

## 安全说明

- ✅ 日记数据使用 AES-256 加密
- ✅ 密码使用 SHA-256 哈希存储
- ✅ 原始日记文件不会上传到公开仓库
- ✅ 子模块可以设为私有仓库
- ⚠️ 密码在客户端解密，请选择强密码

## 项目结构

```
dairy/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions 部署配置
├── diary/                       # 日记子模块（不提交到主仓库）
├── public/
│   └── diary-data.json         # 加密后的日记数据
├── scripts/
│   └── encrypt-diaries.js      # 加密脚本
├── src/
│   ├── components/
│   │   ├── Login.astro         # 登录组件
│   │   └── DiaryList.astro     # 日记列表组件
│   ├── layouts/
│   │   └── Layout.astro        # 基础布局
│   └── pages/
│       ├── index.astro         # 登录页
│       └── diary.astro         # 日记页
├── astro.config.mjs
├── package.json
└── tailwind.config.mjs
```

## License

MIT
